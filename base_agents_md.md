# base_agents_md

実装エージェント（Codex）向けの `AGENTS.md` を**生成**するための仕様とテンプレート。

`AGENTS.md` は手書きしない。プロジェクトの `CLAUDE.md`（読み分け表・手編集禁止一覧・完了の定義）と
`.claude/rules/`（常時適用ルール）から**一方向で生成する生成物**として扱う。

---

## なぜ一方向生成にするのか

実行主体は分業する（役割の定義は `base_dev_pipeline.md` が正）。

| 役割 | 担当 | 書き込む先 |
|---|---|---|
| 設計エージェント（Claude） | Blueprint 構築（受け入れ条件のドラフト・棄却支援・整合性チェック・プロトタイプ生成）、spec 作成、仕様突合 | `docs/`、`src/prototypes/` |
| 実装エージェント（Codex） | 実装 | `src/`、`tests/` |

同じプロジェクトを二つのエージェントが別のファイルを読んで動くため、指示を二重管理すると必ず片方が古くなる。
古いほうを読んだエージェントが「正しいつもりで」仕様と違う実装を出すのが最悪のケースで、
これは実装が通ってしまうので検出も遅れる。

そのため指示のソースは `CLAUDE.md` と `.claude/rules/` に一本化し、`AGENTS.md` はそこからの射影とする。
`AGENTS.md` を手編集すると射影ではなくなり、この保証が消える。

### 参照リンクではなくインライン展開する理由

Codex は `AGENTS.md` を自動で読むが、`.claude/rules/` を自動では読まない。
`AGENTS.md` に「`.claude/rules/security_code.md` を参照」と書いても、実際には読まれずに実装が始まる。
常時適用ルールは**生成時に中身を埋め込む**。リンクで済ませない。

---

## AGENTS.md に含める内容

| # | ブロック | 種別 | ソース |
|---|---|---|---|
| 1 | 役割宣言と書き込み境界 | 固定テンプレ | — |
| 2 | 常時適用ルール（インライン展開） | 抽出 | `.claude/rules/` の全ファイル |
| 3 | タスク別参照表 | 抽出 | `CLAUDE.md` の参照ドキュメント索引（実装系の行のみ） |
| 4 | 手編集禁止ファイル一覧 | 固定テンプレ | — |
| 5 | キーの辿り方 | 固定テンプレ | — |
| 6 | 完了条件と差し戻し様式 | 固定テンプレ | — |

固定テンプレ（1・4・5・6）は下の「生成テンプレート」をそのまま使う。
抽出部（2・3）は生成のたびにソースから作り直す。

### ブロック 2 の抽出ルール

- `.claude/rules/` 配下の全 `.md` を対象に、**規範（守るべきこと）の記述だけ**を取り込む
- 背景・理由の散文は落とす。Codex のコンテキストを占めるだけで実装の判定には使われない
  （理由が必要な判断は差し戻しの対象であり、Codex 側で解決させない）
- ファイル単位で見出しを立て、どのルールファイル由来かを残す（差し戻し時に出典を示せるようにする）
- 1ファイルが長い場合も要約せず、規範のリストは全項目を残す。落とすのは散文だけ

### ブロック 3 の抽出ルール

`CLAUDE.md` の参照ドキュメント索引から、**`src/` または `tests/` に手を入れる作業の行だけ**を残す。

- 残す: コンポーネント実装、ページ・フロー実装、テスト作成、アニメーション実装、a11y 対応、
  フレームワーク固有の規約、SEO・パフォーマンス実装（いずれも配置されている行のみ）
- 落とす: ストーリーの作成・変更、content-list の増減、deck の変更、どのドキュメントを書き換えるかの判断、
  トークンの追加・変更、デザイン案の生成（`/print`）、UX 監査の実行、`CLAUDE.md` 自体の編集
  （いずれも設計エージェントの担当で、Codex には書き込み権限がない）

落とした行の作業を Codex が要求されたら、それは差し戻し対象（ブロック 6）になる。

---

## セットアップ: `.claude/commands/agents-md.md`

以下を `.claude/commands/agents-md.md` として配置する。`/agents-md` で再生成できるようになる。

`AGENTS.md` はソース（`CLAUDE.md`・`.claude/rules/`）が変わるたびに作り直す必要があるため、
生成手順は必ずコマンドとして残す。手順が会話の中にしかないと、次に誰も再生成できず手編集が始まる。

`````markdown
---
description: CLAUDE.md と .claude/rules/ から AGENTS.md を再生成する（手編集禁止の生成物）
---

実装エージェント（Codex）向けの `AGENTS.md` を再生成する。

## 手順

1. 生成仕様を読む
   - `.claude/docs/base_agents_md.md`（含める内容・抽出ルール・生成テンプレート）

2. ソースを読む
   - `CLAUDE.md`（参照ドキュメント索引・手編集禁止ファイル・完了の定義・コマンド）
   - `.claude/rules/` 配下の全ファイル
   - `docs/_rules.md` の「命名」節（存在する場合。slug / component の対応を確認する）

3. 6つのブロックを組み立てる
   - 1・4・5・6 は生成テンプレートをそのまま使う（プロジェクト固有の書き換えは 4 の表のみ）
   - 2 は `.claude/rules/` の規範記述をインライン展開する（参照リンクにしない）
   - 3 は `CLAUDE.md` の索引から `src/` / `tests/` を触る行だけを抽出する

4. プロジェクトルートの `AGENTS.md` に**上書き**で出力する
   - 冒頭に生成物ヘッダ（ソース・再生成コマンド・生成日）を必ず付ける

5. 差分を報告する
   - 前回の `AGENTS.md` からの増減を要約して提示する
   - **ソースに存在しない記述**が既存 `AGENTS.md` にあった場合は、手編集の痕跡として警告する。
     黙って消さない。その内容が必要なら `CLAUDE.md` か `.claude/rules/` に入れてから再生成する

6. 実装は行わない
   - このコマンドの責務は生成まで
`````

---

## 生成テンプレート

`{{ }}` が抽出部。それ以外は原則そのまま出力する。

`````markdown
# AGENTS.md

このファイルは生成物。**手編集禁止**。
ソース: `CLAUDE.md`（参照ドキュメント索引・手編集禁止ファイル・完了の定義）+ `.claude/rules/`
再生成: `/agents-md`（生成仕様は `.claude/docs/base_agents_md.md`）
生成日: YYYY-MM-DD

## 1. 役割と書き込み境界

あなたは実装エージェント。担当は**実装（`src/` と `tests/`）だけ**。

| 対象 | 権限 |
|---|---|
| `src/`、`tests/` | 読み書き可（担当範囲。ただし下の `src/prototypes/` は除く） |
| `src/prototypes/` | **読み取り専用**。設計エージェントが `/print` で生成する検討物。昇格は `src/` へのコピーで行い、原本は触らない |
| `docs/` 配下すべて | **読み取り専用**。仕様・規約・デザインの正本 |
| `.claude/`、`.github/`、`AGENTS.md` | 変更禁止 |
| ハーネス設定（`biome.json` / `.oxlintrc.json` / `tsconfig.json` / `vitest.config.*` / `next.config.*` / `lefthook.yml` / `.storybook/`） | 変更禁止 |
| `docs/specs/` の凍結済み spec、`docs/product/stories/` の受け入れ条件 | 変更禁止（正とする） |
| `.env*`・認証情報ファイル | 読み取りも禁止 |

仕様の変更が必要だと判断した場合も、**ドキュメントを書き換えない**。作業を止めて差し戻す（ブロック 6 の様式）。
実装の都合で仕様を動かせてしまうと、仕様が実装の後追いになり、正本がどこにもなくなる。

これは guidance であって enforcement ではない。強制の実体はサンドボックスのパス権限・
branch protection・CI の禁止パスチェック側にある。

## 2. 常時適用ルール

{{ .claude/rules/ 配下の全ファイルの規範記述をファイル単位でインライン展開する }}

## 3. タスク別の参照先

作業を始める前に、対応するファイルを必ず読む。推測で書き始めない。

{{ CLAUDE.md の参照ドキュメント索引から、src/ または tests/ を触る行だけを抽出した表 }}

この表にない種類の作業（ストーリーの作成、仕様の判断、トークンの変更、ドキュメントの更新）は
担当外。要求されたら差し戻す。

## 4. 手編集禁止ファイル

生成物を手で直すと、次の再生成で消える。ソースを直す必要があるなら差し戻す。

| ファイル | ソース |
|---|---|
| `src/styles/theme.css` | `docs/design/tokens/tokens.json`（`pnpm tokens:build`） |
| `tests/coverage-map.md` | テストコードのアノテーション |
| `docs/design/` 配下 | 上流（Printer） |
| `AGENTS.md`（このファイル） | `CLAUDE.md` + `.claude/rules/`（`/agents-md`） |

## 5. キーの辿り方

`slug` と `component` がこの体系を貫くキー。仕様・実装・テストは同じキーで 1:1 に対応する。
実装対象を渡されたら、まずキーから仕様を引く。

| キー | 仕様 | 実装 | テスト |
|---|---|---|---|
| `slug`（ケバブケース） | `docs/product/stories/{slug}.md` | `src/prototypes/{slug}/`（adopted 案） | `tests/e2e/{slug}.spec.ts` |
| `component`（パスカルケース） | `docs/design/ui/{component}.md` | `src/components/{component}.tsx` | `tests/unit/{component}.test.tsx` |

- 受け入れ条件 ID は `AC-{slug}-{連番2桁}`。実装・テストから参照するときはこの ID をそのまま使う
- 対応する仕様ファイルが存在しない場合は実装しない。差し戻す
- `src/prototypes/{slug}/` は adopted 案が挙動の参考。ただし**正は受け入れ条件**であって
  プロトタイプではない。両者が食い違ったら受け入れ条件に従い、食い違いを差し戻しで報告する

## 6. 完了条件と差し戻し

### 完了条件

以下をすべて満たしたときだけ完了と報告する。

- 対象の受け入れ条件 ID に対応するテストが通っている
- テストの `describe` に slug と条件 ID をアノテーションしている
  （例: `describe('auth-login / AC-auth-login-03', ...)`。`tests/coverage-map.md` の生成元になる）
- テストファーストで進めた（テストを先に書き、赤を確認してから実装した）
- **未カバーの受け入れ条件を増やしていない**（既存の未カバーを解消する義務はないが、
  自分の変更で新たな未カバーを作らない）
- `CLAUDE.md` の「完了の定義」のコマンドが通っている

テストと実装を同じフェーズで同時に変更しない。実装を通すためにテストを緩めない。

### 差し戻しの様式

仕様の曖昧・矛盾・条件不足を見つけたら、推測で埋めずに停止して以下の形で返す。

```markdown
## 差し戻し: {slug}

- 該当条件 ID: `AC-{slug}-{NN}`（複数あれば列挙）
- 種別: 曖昧 / 矛盾 / 条件不足 / 仕様と実装の齟齬 / 担当外の作業
- 内容: 何がどう決まっていないか（1〜3行）
- 判断に必要なこと: 何が決まれば実装できるか
```

「妥当そうな解釈」で進めてはいけない。実装が通ってしまうと、仕様の穴が仕様の穴として残らず、
実装の既成事実として固定される。曖昧なまま実装するより、止まるほうが安い。
`````

---

## 再生成のトリガー

以下を変更したら `/agents-md` を実行する。

- `CLAUDE.md` の参照ドキュメント索引・手編集禁止ファイル一覧・完了の定義
- `.claude/rules/` 配下のファイルの追加・削除・変更（上流からの再同期を含む）

差分が出なければそのまま、出たらコミットする。

---

## 運用ルール

- `AGENTS.md` は生成物。手編集しない。内容を変えたいときはソース（`CLAUDE.md` / `.claude/rules/`）を直して再生成する
- `AGENTS.md` はコミットする（Codex が読むのはコミットされたファイル）
- @codex review が参照するプロジェクト固有のレビュー観点も `AGENTS.md` に手書きしない。
  `.claude/rules/project_conventions.md` の「レビュー観点」節に書けば、ブロック 2 のインライン展開で入る
- 実装の原則（テストファースト・停止して質問する・凍結済み spec を正とする）はブロック 1・6 に含まれる。
  `base_dev_pipeline.md` 側に重複して書かない

---

## 関連ファイル

- `base_dev_pipeline.md`: 設計・実装・レビューの役割分担とパイプライン（AGENTS.md が従う対象）
- `base_codex_review.md`: Codex CLI の認証・コマンド・@codex review の設定
- `base_security_env_setup.md` Step 5: AGENTS.md の文言を強制する enforcement の実体
- `base_claude_md_knowledge.md`: CLAUDE.md 側の設計思想（AGENTS.md との関係を含む）
- `base_testing.md`: テスト層の設計・テストファースト運用

---

最終検証日: 2026-07-30
