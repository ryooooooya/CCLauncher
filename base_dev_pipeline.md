# base_dev_pipeline

開発フローの大方針を定義するファイル。設計・実装・レビューを分離し、決定論的な enforcement の上で回す。

- 設計エージェント（Claude Code）: spec 作成・仕様突合（仕様適合の判定）
- 実装エージェント（Codex CLI）: 設計レビュー・実装
- 品質レビュー: @codex review（GitHub、実装とは別インスタンス）

役割はエージェントの「役割名」で固定し、特定のモデル名には固定しない（モデルは入れ替わる前提）。
Codex CLI の認証・コマンド・レビュー判断基準などの機構は `base_codex_review.md` を参照。

---

## パイプライン全体図

| Phase | 内容 | 担当 | 成果物 |
|---|---|---|---|
| 0. spec 作成 | 背景・スコープ・受け入れ条件・技術設計を spec に書き出す | 設計エージェント + ユーザー | `docs/specs/{機能名}.md` |
| 1. 設計レビュー | spec を Codex にレビューさせ、致命的な問題のみ修正する | 実装エージェント（レビュー役） | レビュー済み spec |
| 2. 凍結 | spec をコミットして凍結。以降の spec 変更はユーザー承認必須 | ユーザー | 凍結された spec |
| 3. 実装 | AGENTS.md に従いテストファーストで実装する | 実装エージェント | 実装 + テスト（PR） |
| 4. 品質レビュー | PR オープンで @codex review が自動で走る。バグ・セキュリティ・保守性を見る | 実装と別インスタンス | 品質レビュー指摘と修正 |
| 5. 仕様突合 | spec の受け入れ条件を1件ずつ実装・テストと突き合わせる | 設計エージェント | 突合結果レポート |

### 品質レビューと仕様突合の分離

Phase 4 と Phase 5 は観点が異なるため統合しない。

- 品質レビュー（Phase 4）: コードとして正しいか。バグ・セキュリティ・パフォーマンス・保守性
- 仕様突合（Phase 5）: spec の通りに作られているか。受け入れ条件の充足・スコープ外変更の有無

品質レビューが通っても「きれいに実装された仕様違反」は検出できない。仕様突合は spec を正として、
受け入れ条件ごとに「どのコード・どのテストが充足を証明するか」を明示する形で行う。

### 品質レビューは実装者と別インスタンスで

実装したインスタンスに自分の成果物をレビューさせない（自己弁護バイアスと文脈汚染を避ける）。
@codex review は PR 単位で新しいインスタンスとして走るためこの原則を満たす。
CLI でレビューする場合も、実装セッションの `resume` ではなく新規セッションで行う。
モデル名は固定しない。「実装と別インスタンスであること」だけを不変条件とする。

---

## CLAUDE.md に追記するゲート

CLAUDE.md には以下のゲートだけを追記する。手順の本体はこのファイルに置き、ゲート発動時に読みに来させる。

```markdown
### 開発パイプライン（必須ゲート）

- 受け入れ条件が3件以上になる機能・変更は、`.claude/docs/base_dev_pipeline.md` のパイプライン
  （spec 作成 → 設計レビュー → 凍結 → 実装 → 品質レビュー → 仕様突合）に従う
- 受け入れ条件が3件未満の小さな変更はパイプラインをスキップしてよい。ただしスキップした事実を
  コミットメッセージまたは PR に明記する
- 凍結後の spec 変更はユーザーの明示的な承認が必要
```

---

## spec テンプレート

`docs/specs/_template.md` として配置し、機能ごとに `docs/specs/{機能名}.md` へコピーして使う。

```markdown
# spec: {機能名}

- 状態: draft | reviewed | frozen
- 作成日: YYYY-MM-DD
- 凍結日: YYYY-MM-DD（Phase 2 で記入）

## 背景・目的

なぜ作るか。解決する問題と、やらないと何が困るか。

## スコープ

- 含む: この spec で実装する範囲
- 含まない: 意図的に外す範囲（外した理由も1行で）

## 受け入れ条件

テスト可能な形で列挙する（3件以上ならパイプライン必須）。各条件は Phase 5 の仕様突合で
「充足を証明するコード・テスト」を対応付けるため、検証手段まで書く。

1. {条件}（検証: 単体テスト / E2E / 手動確認）
2. ...

## 技術設計

`base_preflight.md` Step 5（データモデルの耐久性）の書き出し先はこの節。

- エンティティとリレーションの命名
- トランザクション境界
- ロールバック戦略
- 予測されるインデックス
- 主要な処理フロー・API の入出力

## 変更禁止領域への影響

変更禁止領域（AGENTS.md 参照）に触れる必要があるか。ある場合は理由と、ユーザー承認の記録。

## テスト計画

受け入れ条件をどの層（vitest / Playwright）でカバーするか（`base_testing.md` の層設計に従う）。
```

---

## AGENTS.md 雛形

実装エージェント（Codex）向けの指示。プロジェクトルートに `AGENTS.md` として配置する。
ここに書く内容は guidance であって enforcement ではない。守りの実体は
`base_security_env_setup.md` Step 5（Codex サンドボックス・permissions プロファイル・
branch protection・CI 禁止パスチェック）に置く。

```markdown
# AGENTS.md

## 実装の原則

- `docs/specs/` の凍結済み spec を正とする。spec にない機能を追加しない。spec と矛盾する実装をしない
- テストファーストで進める: 受け入れ条件に対応するテストを先に書き、赤を確認してから実装する
- judgment call が必要になったら停止する: spec の隙間・矛盾・不明点を発見したら、自分で埋めずに
  作業を止めて質問を返す。「妥当そうな解釈」で進めない
- テストと実装を同じフェーズで同時に変更しない（`base_testing.md` のフェーズ分離ルール）

## 変更禁止領域

以下はいかなる理由でも変更しない。変更が必要だと判断した場合は停止して報告する:

- `.claude/`（設定・フック・ルール）
- `.github/`（CI ワークフロー）
- `lefthook.yml` / `biome.json` / `.oxlintrc.json` / `tsconfig.json` / `vitest.config.ts` / `lighthouserc.json`
- `docs/specs/` の凍結済み spec
- `.env*` および認証情報ファイル（読み取りも禁止）

## Review guidelines

- Prisma トランザクションが必要な箇所に抜け漏れがないか確認する
- ファイルアップロード処理でマジックナンバー検証が行われているか
- PII のログ出力がないか確認する
- スキーマ / migration 変更がある場合、既存 API・型定義との整合と後方互換性を確認する
- 新規・変更されたロジック層（Server Action / API / ユーティリティ）に対応するテストがあるか確認する
- 理由の書かれていない lint 抑制コメント（biome-ignore / oxlint-disable）がないか確認する
- spec の受け入れ条件とスコープに照らして、スコープ外の変更が混ざっていないか確認する
```

Review guidelines はプロジェクトの技術スタックに合わせて調整する（@codex review が参照する）。

---

## enforcement（この文言群を機能させる前提）

AGENTS.md・spec の文言はエージェントへの指示にすぎず、破られうる。パイプライン採用時は
以下を先に設定してあること（詳細手順は `base_security_env_setup.md` Step 5）:

1. Codex サンドボックス: workspace-write（または同等の permissions プロファイル）+ ネットワーク遮断
2. 機密ファイル・変更禁止領域のパス権限（permissions プロファイルの `none` / `read` 指定）
3. main の branch protection（直接 push 禁止・PR 必須・required checks）
4. CI の禁止パスチェック（変更禁止領域に diff があれば fail）

運用禁止ルール: `--dangerously-bypass-approvals-and-sandbox` と `--sandbox danger-full-access` での
実装実行は禁止。サンドボックスが理由で失敗する操作（コミット等）は人間または設計エージェント側で行う。

---

## 各 Phase の実行メモ

- Phase 1（設計レビュー）: `base_codex_review.md` の計画レビューコマンドを spec ファイルに対して使う。
  修正後の再レビューは `resume --last` で文脈を引き継ぐ
- Phase 3（実装）: `codex exec` はサンドボックス下で動かす。`.git` が read-only のためコミットは
  人間または設計エージェントが行う
- Phase 4（品質レビュー）: `base_codex_review.md` の「GitHub PR での @codex review」設定で
  "Automatic reviews" をオンにしておく。指摘の採否判断基準も同ファイルに従う
- Phase 5（仕様突合）: 設計エージェントが spec の受け入れ条件を1件ずつ「充足を証明するコード・
  テスト・実行結果」に対応付ける。対応付けられない条件が残る場合は Phase 3 に差し戻す

---

## 関連ファイル

- `base_codex_review.md`: Codex CLI の認証・コマンド・レビュー判断基準・@codex review 設定
- `base_security_env_setup.md` Step 5: Codex 実装経路の enforcement
- `base_preflight.md`: Phase 0 の前提確認（Step 5 の書き出し先は spec の技術設計節）
- `base_testing.md`: テスト層の設計・テストファースト運用のルール

---

最終検証日: 2026-07-14
