# Methods

Optional development methodologies, separate from the default webapp baseline.

Blueprint / Printer is retained with the destination `blueprint-printer/` and its `blueprint/` and `printer/` subdirectories. The coordinated migration is tracked in Issue #8; legacy sources remain in place until then.

See the [architecture](../docs/architecture.md) and [migration map](../docs/migration.md). This index is maintainer-only.
