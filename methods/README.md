# Methods

Optional methodologies, separate from the default webapp baseline.

[Blueprint / Printer](blueprint-printer/README.md) is available through explicit `init --method blueprint-printer`. Its product stories, design assets and prototype history are not installed by default.

See [architecture](../docs/architecture.md) and [migration map](../docs/migration.md).
