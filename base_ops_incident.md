# base_ops_incident

Web サービスの本番障害が起きたときに参照する手順書。障害の最中に考える量を最小にするため、判断フローと準備事項を平時に固定しておく。

## 前提

- 1人開発・低トラフィック段階の Web サービスを想定
- 例示は Next.js + Vercel + Supabase のスタックで書いている。別スタックの場合は同じ役割のサービスに読み替える
- npm サプライチェーン起因のインシデントは `base_security_npm_incident.md` を参照
- 平時の監視・検知の整備は `base_automation_roadmap.md` を参照

## 平時に一度だけやる準備

障害対応の質は、この準備をやったかどうかでほぼ決まる。

- ロールバックを一度実際に試す。Vercel の instant rollback で直前のデプロイに戻し、戻ることを確認する。初見が本番障害、を避ける
- メンテナンス表示の出し方を決めておく（静的ページの差し替えでよい）
- ホスティング・BaaS のステータスページをブックマークする（例: vercel-status.com、status.supabase.com）
- スマホから GitHub / Vercel / Supabase の管理画面にログインできることを確認する。外出先での障害を想定
- ユーザー告知のチャネルを1つ決め、一文テンプレを用意しておく
  - 例: 「現在◯◯が利用できない障害が発生しています。復旧作業中です。復旧次第お知らせします。」

## 発生時の対応フロー

原則: 復旧優先、原因究明は後。障害中に main ブランチでデバッグしない。

1. 直近のデプロイと障害発生時刻に相関があるか確認する
   - 相関あり → 問答無用でロールバック。原因調査は復旧後にやる
2. 相関なし → 自分のコードを疑う前にインフラを見る
   - ホスティング・BaaS のステータスページを確認する
   - インフラ側の障害なら自分にできることはない。告知を出して待つ
3. インフラ正常 → 周辺要因を順に切り分ける
   - ドメイン / DNS（失効・設定変更の有無）
   - 外部 API・外部サービスの障害
4. ここまでで特定できない場合のみコードの調査に入る。修正はブランチを切り、Preview Deployment で確認してからデプロイする

## 告知

- 5分で直らないと判断した時点で、調査より先に告知を出す。テンプレをなぞるだけにしておく
- 復旧したら復旧報を出す。原因の詳細説明は不要

## 収束後

- 「なぜ検知が遅れたか」だけ振り返る。原因の振り返りより先にこれをやる
- 検知の穴が見つかったら `base_automation_roadmap.md` の仕分けを見直して反映する
