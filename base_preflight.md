# base_preflight

プロジェクト着手時・新機能追加時に、実装より先に暗黙の前提を表に出すためのチェック。
伊勢川暁氏の Qiita 記事「20年戦士エンジニアから、素人バイブコーダーの皆様へ」とコメント欄の論点を蒸留したもの。

このドキュメントは毎セッション読み込むためのものではない。
プロジェクト初期化時に一度走らせ、結果を `CLAUDE.md` の「プロジェクト属性」に記録する。
以降は記録された属性が各セッションで参照される。

---

## 基本原則

- セキュリティは CIA トライアド（Confidentiality / Integrity / Availability）の枠で考える
- チェックリスト全部を機械的に埋めない。今回のタスクで実際に効く 1〜3 項目だけを選ぶ
- 「プロトタイプだから省略」と決めた場合は、その事実を記録する。黙って本番に持ち込ませないため

---

## プリフライトの実行手順

Claude Code はプロジェクト初期化時（または新機能追加時）に、以下を順に確認する。

### Step 1. スコープ判定

`CLAUDE.md` の「プロジェクト属性」にスコープが明記されていなければ、ユーザーに確認する。

```
このプロジェクトは実ユーザーに提供される本番運用ですか、
ローカル実験・学習目的のプロトタイプですか？
```

回答に応じて以下を `CLAUDE.md` に追記する。

- `scope: production` → 以降の Step をすべて適用
- `scope: prototype` → Step 2〜6 は省略可。ただし「プロトタイプ宣言日」と「本番化する場合は再 preflight」を明記

### Step 2. LLM 連携の有無

このプロジェクトが LLM API（OpenAI / Anthropic / Azure OpenAI 等）を呼ぶなら、以下をユーザーに確認して `CLAUDE.md` に記録する。

- データ経路：入力はどのプロバイダ・どのリージョンを通るか
- 学習・保持のオプトアウト：デフォルトでは opt-out されていないプロバイダが多い。明示的に設定したか
- プロンプトインジェクション：外部文書・Web ページ・ユーザー投稿を LLM に読ませる場合、すべて敵対的入力として扱う前提か

### Step 3. コスト上限

ループ・バックグラウンド処理・外部 API 呼び出しを含む場合のみ、実装前に以下を確認する。

- 見積もり：`calls per run × runs per day × price per call` を記録する
- ハード上限：プロバイダ側のスペンディングキャップを設定したか
- kill switch：ポーリングループを止める手段が実装に含まれているか

### Step 4. 法務・コンプライアンス

該当する場合のみ確認する。

- スクレイピング：対象サイトの `robots.txt` と利用規約に反していないか
- 個人情報：GDPR・日本の個人情報保護法（APPI）・CCPA のいずれかに触れるか。触れるなら担当者の確認を取ったか
- ライセンス：依存パッケージ・モデルの商用利用可否、GPL 互換性、帰属表示義務を確認したか
- 規制業種：医療・金融・法務・教育は着手前にレギュレーションを確認する

### Step 5. データモデルの耐久性

最初のマイグレーションを書く前に、以下を設計ドキュメントに書き出す。

- エンティティとリレーションの命名
- トランザクション境界：何が原子的にコミットされるべきか
- ロールバック戦略：悪いデータをどう巻き戻すか
- 予測されるインデックス

開発パイプライン（`base_dev_pipeline.md`）採用時は、書き出し先を spec（`docs/specs/{機能名}.md`）の技術設計節にする。
実装詳細は `base_security_code.md` の SQL 取り扱いルールを参照。

### Step 6. 先行事例の調査

新しいライブラリ・パターンを導入する前に、以下を確認する。

- すでに成熟したライブラリで解決されていないか
- 同じ問題の失敗事例（ポストモーテム）が公開されていないか
- プロジェクト内の既存コード・既存パターンを踏襲できないか

フレームワーク固有の調査ルールは `framework_*.md` を参照。

---

## 記録フォーマット

Step 1〜6 の結果は `CLAUDE.md` の末尾に「プロジェクト属性」セクションとして追記する。

```markdown
## プロジェクト属性（preflight の結果）

- scope: production
- LLM 連携: あり / Anthropic API / 学習 opt-out 済み
- 外部コスト: LLM API のみ。月上限 $50 をプロバイダ側で設定済み
- 個人情報: 氏名・メールアドレスを保存。APPI 対応
- スクレイピング: なし
- プロトタイプ省略項目: なし
- preflight 実行日: 2026-01-15
```

属性が変わる機能追加（例：LLM 連携を後から足す、ユーザー登録を追加する）を行う際は、該当 Step のみ再実行してこのセクションを更新する。

---

## 重要な境界線

- 技術的なセキュリティ実装ルール（SQLi・XSS・JWT・シークレット管理等）は `base_security_code.md` に全委譲
- Claude Code の実行環境（サンドボックス / `settings.json` / フック）は `base_security_env.md` に全委譲
- npm サプライチェーン対策は `base_security_npm.md` に全委譲
- この `base_preflight.md` は「実装前に決めるべき非技術的前提」に限る

---

## 出典

- 伊勢川暁「20年戦士エンジニアから、素人バイブコーダーの皆様へ」 https://qiita.com/Akira-Isegawa/items/00f23d206c504db2ac3b
- taiki-yoshida/claude-code-skills `general-prerequisites` https://github.com/taiki-yoshida/claude-code-skills/tree/master/general-prerequisites
