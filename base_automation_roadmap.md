# base_automation_roadmap

運用フェーズに入ったプロジェクトで、定番の自動化を「今やる / 条件付きでやる / やらない」に仕分けるためのロードマップ。判断基準は「設定コストが一度きりで、維持コストがほぼゼロのものから入れる」。

## 前提

- 1人開発・低トラフィック段階の Web サービスを想定
- 例示は Next.js + Vercel + Supabase + Sentry のスタックで書いている。別スタックの場合は同じ役割のサービスに読み替える
- サービスの無料枠・仕様は変わりうる。導入時に必ず現行の条件を確認すること
- これはセットアップ時の手順書ではない。初期セットアップは各 bootstrap guide、npm 周りの自動防御層（Dependabot 等）は `base_security_npm_setup.md` を参照
- 障害発生時の対応手順は `base_ops_incident.md` を参照

## 今やる（設定1時間以内、維持コストほぼゼロ）

### CI: lint + typecheck + build
- GitHub Actions で push/PR 時に `lint` `tsc --noEmit` `build` を回す
- 1人開発は「壊れたまま main に入れて気づかない」が最大の事故源。最優先
- テストがなくてもこれだけで事故の大半は防げる

### Sentry → GitHub Issue 自動作成
- 公式連携の設定のみ。エラーの見落とし防止
- アラートルールは「新規エラーのみ」に絞る。既知エラーの再発まで通知すると即ノイズ化する

### Uptime 監視（UptimeRobot 等の無料枠）
- LP と主要な公開ページの2エンドポイント程度を5分間隔で監視
- ホスティング・BaaS 側の障害はアプリ内のエラー監視では拾えないのでこれが必要

### BaaS 無料枠の自動停止対策（該当する場合）
- 例: Supabase 無料枠は1週間アクセスがないとプロジェクトが一時停止する
- 利用者が見る直前にサービスが死ぬのが最悪シナリオ。GitHub Actions の cron で定期 ping を入れるか、有料化のタイミングを決めておく

### コスト / 課金アラート
- ホスティング・BaaS の使用量アラートと支出上限を設定する（例: Vercel の Spend Management、Supabase の使用量アラート）
- claude-code-action 等で API を使うなら、その課金アラートも同時に設定する
- バグや bot アクセスで請求が跳ねるのは個人開発の定番事故。設定は一度きりなので最初に入れる

### 通知の到達経路
- Sentry の Issue 化・CI の失敗・cron ジョブの失敗が、確実に目に入る場所（Slack / Discord webhook 等）に届くことを確認する
- GitHub の通知メールに埋もれたら、検知の仕組みは存在しないのと同じ
- cron で入れた定期 ping や pg_dump 自体が静かに失敗し続けるのが盲点。ジョブの失敗通知まで含めて経路を確認する

### Dependabot（アラートのみ）
- セットアップ手順は `base_security_npm_setup.md` を参照
- この段階ではセキュリティアラートと依存更新 PR の作成まで。自動マージはまだしない（下記参照）

## 条件付きでやる

### Renovate + パッチ/マイナーの自動マージ
- 条件: CI にビルドだけでなく最低限のスモークテストが入ってから
- テストなしで自動マージすると「動かない依存更新が勝手に main に入る」が起きる。順序が重要
- メジャーバージョン更新は常に手動レビュー

### Playwright スモークテスト（3〜5本だけ）
- 対象: 主要ページが表示される / ログインできる / 中核機能の画面が開く、程度
- 条件: 書くのは1日で終わる範囲に限定する。E2E は増やすほど flaky になり、メンテが「負荷を下げるはずが負荷源」になる典型
- 定期実行（cron）よりまず PR 時実行で十分。トラフィックが増えたら定期実行 + 落ちたら Issue 化を追加

### claude-code-action（PR レビュー）
- 条件: API コストを許容できるなら
- 1人開発のレビュアー不在を補う。自動修正 PR より先に入れる価値がある
- 全 PR で自動発火ではなく、まず @claude メンション運用から始めてノイズを見極める

### DB バックアップの追加保険
- BaaS の自動バックアップはプランに依存する。例: Supabase 無料枠は日次バックアップなし
- 条件: 失うと実害が出るユーザーデータを預かり始めたら必須。GitHub Actions の cron で pg_dump を週次実行する程度で十分
- 仕込んだら一度リストアを試す。復元できないバックアップは存在しないのと同じ

## やらない（今は）

### エラー監視 → 自動修正 PR の完全自動パイプライン
- 理由: エラー流量が少ない段階は手動 @claude で足りる。誤検知エラーにも API コストとレビュー負荷が発生する
- 再検討タイミング: 週に数件以上、同種の単純エラーが安定して流れ込むようになったら

### ユニットテストのカバレッジゲート
- 理由: カバレッジ目標は1人開発では儀式になりやすい。書くべきテストは「壊れたら気づきにくい純粋ロジック」（料金計算・座標計算等）に限定し、ゲートは設けない

### Lighthouse CI / パフォーマンス予算
- 理由: 体感速度は重要だが、自動ゲート化するほど変更頻度が高くない。リリース前に手動で見れば足りる

### CodeQL 等の静的セキュリティスキャン
- 理由: private リポジトリでは有料（GitHub Advanced Security）。Dependabot のセキュリティアラートで当面カバー

### リリース自動化（changelog 生成、semantic-release）
- 理由: 利用者向けリリースノートを出す段階ではない。完全に儀式

### ステージング環境の構築
- 理由: ホスティングの Preview Deployment（Vercel 等）が実質ステージング。別途作るのは DB 分離が必要になってからで、それも BaaS のブランチング機能で足りる可能性が高い

## 自動化以外で、運用開始時に一度だけ確認すること

自動化の仕分けには属さないが、放置すると「気づけない事故」になるもの。

- 障害対応の平時準備: `base_ops_incident.md` の「平時に一度だけやる準備」を実施する（ロールバックの試行、告知テンプレ等）
- ドメインの自動更新: 失効は uptime 監視で検知できるが、防げるのは自動更新設定だけ。支払いカードの有効期限切れにも注意
- アカウント保全: 1人開発では GitHub / Vercel / Supabase アカウントの喪失がサービスの喪失。2FA を有効化し、リカバリーコードをパスワードマネージャー等に保管する

## 導入順序のまとめ

1. CI（lint/typecheck/build）
2. コスト/課金アラート + エラー監視→Issue 連携 + Uptime 監視 + BaaS 停止対策 + 通知経路の確認
3. 一度きりの確認（障害対応の平時準備・ドメイン自動更新・アカウント保全）
4. Playwright スモーク3本
5. Renovate 自動マージ（4の後）
6. claude-code-action レビュー（コスト見ながら）

1〜3で「気づけない事故」が消え、4〜5で「手作業の依存更新」が消える。ここまでで運用負荷の大半は下がる。
