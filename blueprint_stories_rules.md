# ストーリー規約

配置先: `docs/product/stories/_rules.md`
正本: CCLauncher の `blueprint_stories_rules.md`（直接編集せず、上流を更新して再取得する）

ストーリーの書き方・所有権・粒度の規約。テンプレート構造は `_template.md` を参照。

---

## ストーリーとは

提供価値ひとつを単位とするドキュメント。二層構造で扱う。

- **文脈層**（ゴール・フリクション・推奨 UI）: 散文でよい。判断の根拠を豊かに残すことが価値
- **規範層**（定義する挙動）: 仕様書。ID 付き・観測可能・テスト導出可能という規約が厳密にかかる

規範層だけが仕様であり、テストとレビューの参照先になる。文脈層は「なぜそうしたか」を残す場所で、
ここを痩せさせると後から判断を再現できなくなる。

---

## frontmatter の `target` と `pages`

この2つは印刷（`/print`）と整合性チェックの入力になるため、書き方を固定する。

### `pages`

`docs/product/content-list.md` の項目への参照だけを書く。フリーテキストのページ名は書かない。

新規画面のストーリーを書くときも、**先に content-list へページを追加してから参照する**。
この順序にすることで content-list が常に先行し、E2E の網羅チェックリストとして機能し続ける。

### `target`

| 値 | 意味 | 印刷（`/print`）の挙動 |
|---|---|---|
| `new` | 新規画面 | layout spec の領域分割から組み立てる |
| `modify` | 既存画面の改修 | `pages` の対象ページの既存実装を入力に加え、既存を出発点にした差分パターンを生成する |

### `target: modify` は整合性チェックを必ず通す

改修で既存の挙動が変わる場合、**その挙動の定義元ストーリーを書き換えるのが正規ルート**。
改修ストーリー側で同じ挙動を再定義しない（所有権が二重になる）。

改修ストーリーが持てるのは、その改修で**新しく生まれる挙動**だけ。既存挙動の変更は定義元へ、
変わらない挙動は「参照する挙動」へ振る。

---

## 所有権

### 挙動の所有権は一意

ひとつの挙動を定義するストーリーはひとつだけ。他のストーリーの挙動は **参照はできるが再定義はできない**。

```markdown
## 参照する挙動
- ログイン後のリダイレクト: [auth-login](./auth-login.md) `AC-auth-login-03`
```

参照はリンクのみで書く。参照先の内容を要約して書き写すと、そこが二つ目の定義になり、
更新時に片方だけ直って矛盾する。

### 横断フローの所有権

複数ページ・複数機能にまたがるフローは、**そのフローが提供する価値**を持つストーリーが所有する。
通過するページのストーリーは所有しない。

例: 「カートに入れてから購入完了まで」は `checkout` が所有する。`cart` や `product-detail` は
自分のページ内の挙動だけを定義し、checkout フローの挙動は参照する。

### 矛盾は不可

同じ挙動に対して異なる期待値を書いたストーリーが二つあってはならない。
矛盾を見つけたら実装より先にストーリーを直す。どちらが正かの判断は人間が行う。

---

## 受け入れ条件

### 条件 ID

`AC-{slug}-{連番2桁}` の形式。採番後は変更しない（テストのアノテーションから参照されるため）。
条件を削除した場合、その番号は欠番のままにして再利用しない。

### 粒度: 外から観測可能な挙動まで

条件は「ユーザーまたはテストが外から観測できること」だけを書く。実装詳細には振らない。

良い条件:
- `AC-search-01`: 検索欄に2文字以上入力すると、300ms 後に候補リストが表示される
- `AC-search-02`: 候補が0件のとき、「該当なし」と代替アクションが表示される

悪い条件と、なぜ悪いか:
- 「`useDebounce` フックで300msデバウンスする」 → 実装詳細。実装を変えたら仕様が壊れたことになる
- 「検索が高速に動作する」 → 観測不能。何をもって合格かがテストに落ちない
- 「適切にエラーを処理する」 → 曖昧。適切の定義がどこにもない
- 「候補リストの各行は高さ40pxで表示される」 → 粒度過剰。見た目は ui spec の管轄

### 形式

Given-When-Then で書く。前提が自明な場合は When-Then でよい。

```markdown
- `AC-search-01`: 検索欄が空の状態で（Given）、2文字入力すると（When）、300ms 後に候補リストが表示される（Then）
```

### 過剰な条件は落とす

網羅は AI が得意なので、ドラフトは過剰に出る。人間のレビューは「増やす」のではなく「落とす」ために行う。
落とす基準は、実装詳細・観測不能・粒度過剰・他ストーリーの再定義の4つ。

---

## プロトタイプのライフサイクル

1. `/print {slug}` で `src/prototypes/{slug}/` に複数パターンを Storybook story として生成する
   （印刷コマンドの仕様は `.claude/docs/base_print.md`。手で書き起こさない）
2. 人間が触って評価し、adopted を1つ選ぶ。frontmatter に adopted と選定理由1行を書く
3. 受け入れ条件を、文脈層と adopted プロトタイプを素材にドラフトする
4. 実装完了後、プロトタイプはアーカイブする（削除はしない。検討過程として残す）

adopted が決まったあとに `/print` を再実行しても adopted のファイルは上書きされない。
判断済みのものを生成で壊さないため。刷り直したいときは adopted を外してから実行する。

実装完了後の正は **受け入れ条件と実装** であって、プロトタイプではない。
プロトタイプと実装がずれても、プロトタイプは直さない。

---

## Story 作成ルール

プロトタイプは Storybook の story として置く。Storybook 自体のセットアップ（MCP server / Manifest 連携）は
`.claude/docs/base_storybook.md` の管轄で、**story の書き方はこのファイルが持つ**。

### 基本方針

- 1 story = 1 概念。複数のバリエーションを1つの story にまとめない
- CSF 3 形式（`satisfies Meta<typeof Component>` + `StoryObj`）
- named export（default export は meta のみ）
- story 名は状態やユースケースを表す名前にする（`Primary`, `Disabled`, `WithIcon`）
- コンポーネントと同じディレクトリに `.stories.tsx` を置く（コロケーション）

### プロトタイプの story

- 配置は `src/prototypes/{slug}/`。ファイル名はパターンを識別できる名前（`a-card-grid.tsx` 等）
- 本番と同一リソースのみで組む。実コンポーネントと semantic トークンだけを使い、
  色・間隔の直書きをしない。モックデータは props か `args`（fixture）で注入する
  （昇格時に注入元を実データへ差し替えるだけで本実装の出発点になる状態を保つ）
- story のタイトルは `Prototypes/{slug}` で揃える。人間が並べて比較できることが目的
- 各パターンの story に `@summary` で「この案が何を優先しているか」を1行書く。採否判断の材料になる
- adopted が決まったら、非採用パターンの story に `tags: ['!manifest']` を付けてエージェントの視界から外す
  （ファイルは残す。検討過程の記録であって、実装の参照先ではないため）

### JSDoc（必須）

AI エージェントが Manifest 経由でコンポーネントを理解するための最重要情報源。

- コンポーネントの export に description と `@summary`（用途を簡潔に。実装詳細ではなく「いつ使うか」）
- すべての Props に description
- 各 story に description と `@summary`。「何を」ではなく「なぜこの状態を使うか」

### Manifest の管理

- エージェントに見せる必要がない story（アンチパターン例・deprecated・非採用プロトタイプ）には `tags: ['!manifest']`
- MDX も `<Meta tags={['!manifest']} />` で除外できる
- 不要な情報が多いとエージェントの精度が落ちる。必要なものだけ含める

### テスト

- インタラクションテストは play function で書く
- story を書いたら `run-story-tests` で動作確認する
- 受け入れ条件を検証する story には、describe/story 名に条件 ID を含める（`tests/coverage-map.md` の生成元になる）

---

## slug 命名規約

- ケバブケース。英小文字と数字とハイフンのみ
- 機能領域を接頭辞にする（`auth-login`, `auth-signup`, `cart-add`）
- 動詞を含めて「何ができるか」がわかる名前にする（`product` ではなく `product-search`）
- ページ名ではなく提供価値で命名する。1ページに複数の価値があれば slug は複数になる
- 採番後の rename は、プロトタイプ・E2E テスト・content-list の参照を同時に直す場合のみ許可する

---

## レビュー観点（AI が機械的に検査する）

- 所有権の一意性: 他ストーリーの定義を再定義していないか
- 同一挙動への期待値の矛盾
- 実装詳細への踏み込み・観測不能な条件・粒度過剰
- 参照リンク切れ（slug、条件 ID、component 名）
- frontmatter の必須項目欠落（adopted 未決定のまま実装に進んでいないか）
- `pages` が content-list.md の項目を指しているか（フリーテキスト・参照切れの検出）
- `target: modify` のストーリーが既存挙動を再定義していないか

検出して人間に返すところまでが AI の担当。どちらを直すかの判断は人間が行う。
